{
  "name": "authloop",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "check-types": "turbo run check-types",
    "codegen": "turbo run codegen",
    "test": "turbo run test",
    "release": "pnpm build && pnpm test && pnpm check-types"
  },
  "devDependencies": {
    "turbo": "^2.8.17",
    "typescript": "5.9.3",
    "vitest": "^4.1.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
